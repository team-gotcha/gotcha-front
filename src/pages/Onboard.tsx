import React from "react";
import styled from "styled-components";

import test from "../assets/videos/test.mp4";

const Onboard = () => {
  return (
    <div>
      <h1>This is Onboard</h1>
      {/* <video muted autoPlay loop>
        <source src={test} type="video/mp4" />
      </video> */}
    </div>
  );
};

export default Onboard;
